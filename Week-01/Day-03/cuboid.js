'use strict';
exports.__esModule = true;
// Write a program that stores 3 sides of a cuboid as variables (floats)
// The program should write the surface area and volume of the cuboid like:
//
var l = 50;
var w = 20;
var h = 10;
var area = w * l * 2 + 2 * l * h + 2 * w * h;
var volume = l * w * h;
console.log(area);
console.log(volume);
