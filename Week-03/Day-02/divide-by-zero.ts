'use strict'
export{};

let fs = require("fs");

function exerc1 (num: number) {
    if (num === 0) {
        console.log("Fail");
    } else {
        console.log(num / 10);
    }
   
}

exerc1(0);

